apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: test
  namespace: flux-system
spec:
  interval: 60m
  path: ./clusters/my-cluster
  prune: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age
