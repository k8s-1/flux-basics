apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: test
  namespace: flux-system
spec:
  interval: 60m
  path: ./clusters/my-cluster/sops
  prune: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age
